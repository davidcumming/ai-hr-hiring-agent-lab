# Deviation Log — slice-e1-candidate-evaluation-council (Stage 9)

> Captures where implementation reality diverged from the slice spec / implementation plan, per Process Doc §16 and the `implementation-deviation-capture` discipline. None of these deviations weakens an acceptance criterion or a hard boundary; all are covered by tests.

| ID | Deviation | Spec/Plan reference | Reality | Reason | Disposition |
|---|---|---|---|---|---|
| DEV-001 | Python 3.10 instead of plan's 3.12 target | implementation-plan.md §stack (Python 3.12, ≥3.11) | `requires-python >=3.10`; `tomli` fallback for `tomllib`; CI pins 3.10 | Execution sandbox provides Python 3.10.12; code kept 3.10-compatible so the deterministic suite is runnable in the verification environment | Accepted. No behavioural impact; noted in `pyproject.toml`. Revisit pin when a 3.12 runtime is standard |
| DEV-002 | HTTP status mapping changed from plan's provisional table | implementation-plan.md §3.4 (provisional 422/409, 403-for-unauthenticated) | Business outcomes (`completed`/`blocked`/`validation_failed`) → HTTP 200 with envelope status; malformed body → 400; missing identity → 401; authenticated non-`hr` → 403 (envelope `unauthorized`) | Stage 6 condition C-COND-1: source API-contracts doc §4 already decides the mapping; plan's table was explicitly provisional (AF-004) | Accepted (architecture-check directed). Locked by DT-007/DT-009/DT-014 |
| DEV-003 | Provider trace metadata extended with nullable `eval_run_id` | implementation-plan.md §3.15 (trace_id placeholder only) | `trace_id` AND `eval_run_id` both present as nullable placeholders, plus `orchestration_version` (`council-composition-v1`) | Stage 6 condition C-COND-2: Foundry companion §8 requires trace ID *and* eval ID in the seam contract | Accepted. Asserted by DT-013 mock-parity tests |
| DEV-004 | Quality gate 3 (evidence-per-score) interpretation | spec FR-008 ("evidence-per-score") read literally could conflict with BR-013/DT-015 | A criterion with an explicit missing-evidence note and a conservative floor score **passes** gate 3; fabricated or unresolvable citations still fail | BR-013 requires missing evidence to produce a note + trigger, never a fabricated citation; a literal gate would force exactly the fabrication BR-013 forbids | Accepted, documented in `gates/quality_gates.py` docstring. Covered by DT-006/DT-015. Flag for eval-contract reconciliation at Stage 12 current-state docs |
| DEV-005 | OQ-002 resolved as "include auto_escalate" | spec BR-005 left both branches open | `escalation_policy = auto_escalate` is implemented behind server-side config (default remains `record_only`); `escalation_provenance` enum `none\|configured_escalated\|policy_triggered` recorded on every record | Stage 5 decision: Mode C executor must exist anyway for BR-003 and triggers are computed every run, so the branch met the PO's "cheap and clean" bar (§2.1.3) | Accepted (PO pre-authorized either branch). Implemented branch of AC-008/DT-004 asserted |
| DEV-006 | Plan template structure | implementation-plan-builder template | Plan added a §3 "Design Specification" section and renumbered subsequent sections | Greenfield repo needed module-level design, not just changed-file lists | Accepted; recorded in the plan itself |

No other deviations. The spec remains intent; code, tests, and this log are implementation truth as of 2026-06-11.
