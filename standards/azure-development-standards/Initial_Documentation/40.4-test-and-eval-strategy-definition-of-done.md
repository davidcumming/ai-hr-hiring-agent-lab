# 40.4 Test and Eval Strategy / Definition of Done

> **Status:** Stub — not yet authored; see §40 of the Process Doc (`gentech_slice_based_development_process_revised.md`, §40.4).
> **Purpose:** Consolidates the testing and eval policy already defined in the Process Doc — deterministic and live-model eval expectations, the standard versus high-assurance packages, repeated-run thresholds and human-review requirements, cost and latency budgets, blocking versus non-blocking failures, model/prompt/tool/orchestration version tracking and mandatory re-eval triggers, and the Definition of Done.

## Where the content currently lives

This document consolidates existing process sections; until authored it points to them directly. In `gentech_slice_based_development_process_revised.md`:

- **§18** — standard versus high-assurance eval packages; deterministic and live-model eval expectations.
- **§19.1** — repeated-run thresholds and human-review requirements (note R5: record binomial confidence / "treat as directional" for Standard-tier results).
- **§20** — cost and latency budgets.
- **§22** — blocking versus non-blocking failures.
- **§19.2, §21** — model/prompt/tool/orchestration version tracking and mandatory re-eval triggers.
- **§30** — the Definition of Done.

Related: 40.2 §1.2 (principle 8, deterministic-mock + live-eval) and the Foundry observability/evaluations harness direction (R13) bear on the eval tooling this document will reference.

## What this document must define when authored

A single consolidated test-and-eval strategy and Definition of Done for the project, drawn from the sections above, plus the project's chosen eval harness (per R13, default to Foundry projects' tracing/evaluations + the `azure-ai-evaluation` SDK).
