# 40.3 Slice Specification Template

> **Status:** Stub — not yet authored; see §40 of the Process Doc (`gentech_slice_based_development_process_revised.md`, §40.3).
> **Purpose:** Defines the required structure for slice specs. It must capture the functional/business/process content listed in §12.2 and must also embed the slice-sizing checks (§11.1) and the split-the-slice triggers (§11.2), so every spec is sized before it proceeds.

## Where the content currently lives

This template is **owned by the `slice-spec-generator` skill/template** (per §40.3). Until this document is authored as a standalone artifact, the operative source is:

- **`Skills/.agents/skills/slice-spec-generator/`** — the template that owns slice-spec structure.
- **§12.2** of `gentech_slice_based_development_process_revised.md` — the functional/business/process content a spec must capture.
- **§11.1 (slice sizing checks)** and **§11.2 (split-the-slice triggers)** — the sizing rules every spec must embed.

## What this document must define when authored

The required slice-spec section structure, with the §12.2 content embedded and the §11.1 sizing checks and §11.2 split triggers built in so a spec cannot proceed un-sized.
